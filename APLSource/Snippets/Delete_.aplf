 msg←sourceFile Delete_ ns;filenames;snippetNames;ind;name;msg;txt;CR
 txt←''ns.Switch ns._1
 (filenames snippetNames ind name msg)←CollectSnippetInfo'to be deleted'txt sourceFile
 :If 0<≢name
     :If FilesAndDirs.DeleteFile filenames[ind]
         msg←'Snippet deleted: ',name
     :Else
         CR←⎕UCS 13
         msg←'Something went wrong, could not delete: ',CR,⊃{⍺,CR,⍵}/filenames[ind]
     :EndIf
 :EndIf
